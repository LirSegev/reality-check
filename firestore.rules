rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{gameId} {
      allow get;
      allow list, write: if request.auth.uid != null && request.auth.token.firebase.sign_in_provider != 'anonymous'
      
      match /intel/{id} {
      	allow read: if request.auth.uid != null;
        allow create: if request.auth.uid != null && request.auth.token.firebase.sign_in_provider != 'anonymous'
      }
      
      match /players/{player} {
      	allow update: if request.auth.uid == request.resource.data.uid;
        allow read, create;
      }

      match /chat/{id} {
        allow read, create: if request.auth.uid != null;
      }
    }
  }
}